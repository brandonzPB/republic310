import React, { useContext } from 'react';
import { GlobalContext } from '../contexts/GlobalContext';

const Body: React.FC = () => {  
  return (
    <div id="test">
    </div>
  )
}

export default Body;
